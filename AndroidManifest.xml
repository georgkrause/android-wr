<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.georgkrause.depreciation"
    android:installLocation="auto"
    android:versionCode="0040006"
    android:versionName="0.4.1" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:smallScreens="true" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".linear.CalculatorActivity"
            android:label="@string/title_activity_rechner" >
        </activity>
        <activity
            android:name=".linear.ResultActivity"
            android:label="@string/title_activity_ergebnis" >
        </activity>
        <activity android:name=".mixed.CalculatorActivity" >
        </activity>
        <activity android:name=".mixed.ResultActivity" >
        </activity>
    </application>

</manifest>